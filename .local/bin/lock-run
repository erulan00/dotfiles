#!/usr/bin/env bash
set -euo pipefail

"$HOME/.local/bin/i3lock-widgets"

pkill -u "$USER" -USR2 dunst || true
if command -v eww >/dev/null 2>&1; then
  eww -c "$HOME/.config/eww/lockscreen" close-all || true
fi
